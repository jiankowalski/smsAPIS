	<form action="/situacaoMensagem/update/<%=situacaoMensagem.id%>" method="POST" class="edit">
	<div class="form-group">
		<h2> editing a situacaoMensagem... </h2>
		<% if(flash && flash.err) { %>
<ul class="alert alert-success">
<% Object.keys(flash.err).forEach(function(error) { %>
<li><%- JSON.stringify(flash.err[error]) %></li>
<% }) %>
</ul>
<% } %>
		
	<div class="form-group" style="width: 400px;">
	<label>name</label>
	<input value="<%= situacaoMensagem.data %>" name="data" type="text" class="form-control"/>
	</div>

		</div>
		<input type="submit" value="Update SituacaoMensagem" class="btn btn-default"/>
		<a href="/situacaoMensagem/show/<%=situacaoMensagem.id %>" class="btn btn-danger">Cancel</a>
	</form>
	
	